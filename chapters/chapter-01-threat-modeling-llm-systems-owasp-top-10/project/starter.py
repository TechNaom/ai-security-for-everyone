"""
Chapter 1 Project: Preview -- No Hands-On Project Yet

Module 1's Level 1 (Guided) project -- threat-model a real, given LLM
feature end to end -- ships after Chapter 2, not after Chapter 1. This
chapter builds the OWASP Top 10 framework and walks it against a worked
example (GreenCart/Aurora); Chapter 2 applies that framework to a real,
given feature, and the L1 project needs both pieces in place before it
can ask you to do the same thing independently.

See project/index.html (or project/README.md) for the full preview of
what the L1 project will ask you to do.

Run this file to see the same summary at the command line:

    python3 starter.py
"""


def main():
    print("Chapter 1 Project: Preview")
    print("=" * 60)
    print(
        "This chapter does not ship a hands-on project. The Level 1\n"
        "(Guided) project for Module 1 -- threat-model a real, given\n"
        "LLM feature end to end -- unlocks after Chapter 2, once both\n"
        "the framework (this chapter) and its applied practice\n"
        "(Chapter 2) are in place.\n"
    )
    print("See project/index.html for the full preview.")


if __name__ == "__main__":
    main()
